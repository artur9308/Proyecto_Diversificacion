version: '3.8'

services:
  mysql:
    image: mysql:8
    container_name: milpa_mysql
    restart: always
    env_file:
      - .env
    ports:
      - "3307:3306"
    volumes:
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql